---
name: sayelf-qingyubiao
display_name: 山野精灵.晴雨表
display_name_en: SAYELF Weather Calendar
description: 全国通用单文件天气晴雨表：逐日国标气象符号图案、年度/月度/上下午视图、行业场景作业决策、PNG/PDF 导出与一键分享、授权订阅。当用户提到 晴雨表、天气日历、山野精灵、场景决策、作业停工建议、天气记录导出 时使用。
description_zh: 山野精灵.晴雨表 —— 把每天的降水画成国标气象符号日历，支持全国定位、未来三天预报（虚线显示、到点自动更正为实测）、按行业阈值给出可作业/有条件/停工建议，可导出 PNG/PDF、生成微信/抖音/小红书分享长图。数据保存在本地浏览器，无需注册登录。
description_en: A single-file weather calendar app that renders daily precipitation as GB/T-28592 weather icons, with year/month/AM-PM views, industry-specific work-decision advice (proceed/caution/stop), PNG/PDF export, and share-card generation for WeChat/Douyin/Xiaohongshu. Data stays in the local browser.
category: tools
version: 1.0.0
author: 山野精灵 SAYELF
---

# 山野精灵.晴雨表

把天气画成一本可以翻的日历。单文件 HTML 应用（`templates/山野精灵.晴雨表.html`），无后端、无账号，双击或在线打开即用。

## 技能指令

当用户需要做以下事情时，按对应步骤执行：

### 1. 打开 / 安装应用
1. 应用本体在 `templates/山野精灵.晴雨表.html`（约 110KB 单文件）。
2. 本地使用：把文件复制到用户桌面或任意目录，用系统默认浏览器打开（`start "" "文件绝对路径"`）。
3. 在线使用：如存在已发布的在线链接，直接给出链接（手机浏览器打开后可"添加到主屏幕"）。
4. 首次打开会请求浏览器定位授权，自动定位到用户所在区县；失败时点页头「重新定位」改用 IP 定位。

### 2. 讲解功能
| 功能 | 位置 | 说明 |
|---|---|---|
| 年度视图 | tab 1 | 12 行月 × 31 列日矩阵，点月份跳月度、点某天跳上下午 |
| 月度视图 | tab 2 | 按周对齐的月历 |
| 上下午分段 | tab 3 | 上午 05-12 时 / 下午 12-21 时，点某天可填气象局官方通报值校订 |
| 场景决策 | tab 4 | 按行业阈值给出 可作业/有条件/建议停（订阅功能） |
| 导出 | 导出按钮 | PNG / PDF（A4 可打印）、数据 JSON 备份 |
| 一键分享 | 分享按钮 | 生成微信/朋友圈/抖音/视频号/小红书推荐尺寸长图+文案 |

数据来源：Open-Meteo 数值再分析（ECMWF/ICON），非官方发布值；未来三天为数值预报（虚线显示），时间一到自动被逐时实测覆盖，每次打开/每 30 分钟自动重取近 7 天。

### 3. B 端定制（改行业阈值）
1. 打开 HTML，在脚本内搜索 `SCENES`——每个场景是一份 `rules:[{max,level,txt}]` 阈值表，从严到宽排列，取第一条 `mm<max` 命中；`level` 取 ok（可作业）/ watch（有条件）/ stop（建议停）。
2. 内置场景：通用参考、建筑施工、农业种植、户外物流。为某客户定制时：改前先备份（复制一份 `.bak.日期`），阈值必须来自客户自己的作业规程（如"降雨 ≥1mm 停止混凝土浇筑"），改完刷新页面即生效。
3. 详细定制步骤与字段说明见 @references/customization.md。

### 4. 授权与订阅
- 首次打开起 15 天免费试用，全部功能可用；到期后查看/记录/数据导出永远免费，仅 导出图片/PDF、打印、一键分享、另存为、场景决策 需要授权码。
- 用户在订阅对话框（页头订阅状态胶囊）填入形如 `QY-XXXX-XXXX` 的授权码点「激活」即解锁。
- 授权码由作者（山野精灵 SAYELF）签发；购买与支持联系方式见订阅对话框内的付款说明。

## 排障
1. 白屏/打不开 → 确认文件完整（约 110KB）；换 Chrome/Edge 打开。
2. 数据不更新 → 检查网络；跨域报错时在文件目录起本地服务：`python -m http.server 8000`，再访问 `http://localhost:8000/山野精灵.晴雨表.html`。
3. 定位不准 → 「重新定位」；数据按地点隔离存放，切换城市不会串用记录。
4. 数据迁移 → 导出对话框里「数据 JSON」，在另一台电脑导入；数据只存在本地浏览器，清浏览器缓存会丢记录，请定期导出备份。

## 边界
- 单文件应用的固有限制：页面关闭后不在后台记录，下次打开时按历史逐时数据自愈补录。
- 授权校验为客户端软锁，定位是"防误用"而非"防破解"。
- 天气数据为再分析/数值预报，仅供记录与决策参考，不构成官方气象证明（官方值可用「上下午分段」里的校订功能手工填入）。
